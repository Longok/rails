<h1>Show products</h1>

<p><%= image_tag @product.display_image if @product.image.attached? %></p>
<p><%= @product.name %></p>
<p><%= @product.price %></p>

| <%= link_to "Delete", @product, method: :delete, data: {confirm: "You sure?"} %>